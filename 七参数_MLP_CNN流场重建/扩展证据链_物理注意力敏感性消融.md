# 扩展证据链：物理约束、注意力、敏感性与消融

## 1. 物理约束

当前 MLP-CNN 主模型已真实使用：

- `u/v/p/T` 场监督；
- `L_eta` 指标一致性损失；
- 损失函数：`L_u + L_v + L_p + L_T + alpha*L_Nu + beta*L_f + gamma*L_eta`。

证据文件：

- `02_MLP_CNN流场重建训练.py`
- `results/mlp_cnn_field_multiloss_e10/metrics.json`
- `results/mlp_cnn_field_multiloss_e10/config.json`

边界：

- 当前 MLP-CNN 未完成完整 PDE-PINN 残差训练。
- `L_cont`、`L_energy`、`L_bc` 只能写作扩展设计或后续工作。

## 2. 注意力机制

旧工程中存在 CBAM/PI-CNN-CBAM 脚本和结果，但属于几何掩码或图像输入路线，不属于当前最终 MLP-CNN 流热场重建主线。

可作为历史对照的证据：

- `七参数_几何掩码代理优化/ablation_results_1000/ablation_summary.json`
- `七参数_几何掩码代理优化/kfold_results_1000/kfold_summary.json`
- `模型脚本/物理信息CNN_模型.py`

边界：

- 当前主模型尚未训练 `MLP-CNN + Attention` 独立版本。
- 论文中不能把旧 CBAM 数值写成当前 MLP-CNN 注意力消融结果。

## 3. 参数敏感性分析

新增脚本：

- `06_参数敏感性分析.py`

输出目录：

- `analysis_results/sensitivity/`

输出文件：

- `pearson_correlation.csv`
- `spearman_correlation.csv`
- `rf_importance.csv`
- `local_perturbation_eta.csv`
- `sensitivity_bar_eta.png`
- `sensitivity_bar_Nu_f.png`
- `local_perturbation_eta.png`
- `sensitivity_summary.json`
- `sensitivity_summary.md`

主要结果：

- eta 随机森林置换重要性最高参数：`Ts = 1.361628`
- eta 随机森林置换重要性第二：`theta = 0.231464`
- eta 随机森林置换重要性第三：`Tb = 0.147826`
- DE 最优点附近局部扰动最敏感参数：`Ts`，`eta_range = 0.044242`
- 局部扰动第二：`Tb`，`eta_range = 0.017418`
- 局部扰动第三：`theta`，`eta_range = 0.014892`

边界：

- 该结果是统计敏感性和代理模型局部扰动分析。
- 不能写成固定其他参数的严格 CFD 单因素扫描。

## 4. K 折验证

新增脚本：

- `07_MLP_CNN_K折验证.py`

输出目录：

- `results/mlp_cnn_kfold_3_e5/`

输出文件：

- `kfold_summary.json`
- `kfold_summary.md`
- `kfold_metrics.csv`
- `fold_01/metrics.json`
- `fold_02/metrics.json`
- `fold_03/metrics.json`

3 折短训结果：

| 指标 | 均值 | 标准差 |
|---|---:|---:|
| Nu_R2 | 0.935270 | 0.006772 |
| Nu_MAE | 1.034223 | 0.008901 |
| f_R2 | 0.897467 | 0.018731 |
| f_MAE | 0.005657 | 0.000342 |
| eta_R2 | 0.876571 | 0.009938 |
| eta_MAE | 0.033816 | 0.000945 |
| field_RMSE | 2.836312 | 0.087924 |
| T_MAE | 2.100128 | 0.028304 |

边界：

- 每折只训练 5 epoch，用于稳定性补充验证。
- 不替代 `mlp_cnn_field_multiloss_e10` 的 10 epoch 正式结果。

## 5. 消融实验表述

可写成真实结果的对照：

| 对照 | 状态 | 证据 |
|---|---|---|
| 旧 1000 组 ANN/MLP baseline | 已完成 | 旧复核 eta = 1.420111 |
| 当前 MLP-CNN 主模型 | 已完成 | eta_R2 = 0.956113，CFD eta = 1.447895 |
| 当前 MLP-CNN 3 折短训 | 已完成 | `results/mlp_cnn_kfold_3_e5/` |
| 旧 PI-CNN-CBAM/CBAM | 已完成但旧路线 | 仅作历史对照 |
| MLP-CNN + Attention | 未训练 | 只能写成扩展设计 |
| MLP-CNN + PDE Physics Loss | 未训练 | 只能写成扩展设计 |

## 6. PPT 更新

已重新生成：

- `MLP_CNN流热场重建_论文答辩PPT.pptx`

新增页面：

- 参数敏感性分析；
- K 折短训稳定性验证；
- 物理约束与注意力机制。
