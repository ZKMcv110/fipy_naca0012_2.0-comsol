# 论文数据证据链

> 本文件用于把论文中的图、表、关键结论与项目中的真实文件对应起来。凡未列出可靠来源的数据，正文中不得写成确定性结论。

## 1. 当前主稿

- 原主稿文件：`大论文初稿/基于物理信息CNN的翼型管翅片散热器论文.md`
- 七参数答辩稿：`大论文初稿/基于物理信息CNN的翼型管翅片散热器论文_七参数版.md`
- 旧版备份：`大论文初稿/完整论文初稿.md`
- 答辩材料：`大论文初稿/毕业答辩PPT制作文档_可复制到其他平台.md`
- 图表追踪：`大论文初稿/七参数版图表追踪清单.md`
- 审稿人复查：`大论文初稿/七参数版审稿人复查报告.md`
- 当前写作原则：
  - 原主稿不再继续改动，避免破坏既有六参数版本。
  - 后续答辩稿以七参数版为准。
  - 七参数版中涉及非零倾斜角的正式优化结论必须写成“已完成小批量补样和轻量评估，但尚未形成完整非零theta优化结论”，不能写成已由现有样本证明完整敏感性规律。
  - 七参数版正式优化结论采用 `optimization_results_pi_cbam/` 与 `validation_results_pi_cbam/` 的结果；早期 `optimization_results/` 与 `validation_results/` 仅作为程序链路验证记录。
  - 七参数版摘要和第7章已统一为“七参数几何掩码输入 + 四模型消融 + 差分进化 + COMSOL复核”主线，不再把GAP/五模型作为核心创新点。
  - 参考文献补充当前暂停；不得继续新增、替换或伪造参考文献条目。

## 2. 二维六参数 CFD 数据

- 标签表：`consol_cfddata/labels.csv`
- 字段：`case_id, Ta, Twa, Tb, Ts, Tt, Tad, theta, Nu, f, delta_p, delta_T, Q_total, image_complete, failure_reason`
- 标签统计口径：
  - 样本数：500。
  - `Nu` 范围：12.075464 至 24.846997，均值约18.668429。
  - `f` 范围：0.024865 至 0.046335，均值约0.034425。
  - `delta_p` 范围：0.372976 Pa 至 0.695022 Pa，均值约0.516368 Pa。
  - `Q_total` 范围：6527.641885 至 12216.371391。
- 摩擦因子定义来源：`comsol单次执行脚本.py` 与 `七参数_几何掩码代理优化/02a_七参数COMSOL单工况求解.py` 中均按 `f = delta_p / (0.5 * rho * u_in_set**2)` 计算，不含额外的排数归一化项。
- 网格说明限制：当前批量标签没有保存同一工况粗/中/细三套网格对比数据，因此论文第3章不应写成已完成定量网格无关性验证，只能说明网格策略与局部加密方法。
- 用途：
  - 二维样本统计
  - 参数分箱趋势
  - 旧六参数模型训练
  - 旧 CFD 云图 PI-CNN-CBAM 对比
- 风险：
    - 该表不包含真实 `theta` 扰动样本。

## 2.1 参数敏感性/参数影响分析

- 分箱趋势图生成脚本：`prepare_thesis_figures_and_insertions.py`
- 分箱方式：`pd.qcut(..., q=6)`，即6个等频分箱。
- 分箱趋势图：
  - Nu：`大论文初稿/figures/fig3_10_nu_response.svg`
  - f：`大论文初稿/figures/fig3_11_f_response.svg`
  - eta：`大论文初稿/figures/fig3_12_eta_response.svg`
- 随机森林重要性：
  - 图：`大论文初稿/figures/fig3_13_parameter_importance.svg`
  - 数据：`大论文初稿/figures/fig3_13_parameter_importance.csv`
  - 脚本配置：`RandomForestRegressor(n_estimators=300, random_state=42, n_jobs=1)`
  - 指标含义：`feature_importances_` 为回归树节点不纯度下降贡献的归一化度量，可理解为各参数对均方误差下降的相对贡献。
- 当前真实重要性排序：
  - Nu：`Tt=0.540`, `Ts=0.366`, `Tb=0.073`
  - f：`Tb=0.528`, `Tt=0.270`, `Ts=0.147`
  - eta：`Tt=0.569`, `Ts=0.378`, `Tb=0.032`
- 写作限制：
  - 当前敏感性结果是“样本分箱均值趋势 + 随机森林特征重要性”，不是固定其余参数的严格单因素CFD扫描。
  - 不得写“分箱均值完全消除了其他参数干扰”。
  - 当前 `theta=0`，不得写已评估非零倾斜角敏感性。
  - 从该表扩展出的 `theta=0` 七参数标签只能用于流程验证，不能证明倾斜角影响规律。

## 3. 旧 CFD 云图 PI-CNN-CBAM 模型

- 模型定义：`模型脚本/物理信息CNN_模型.py`
- 训练脚本：`模型脚本/物理信息CNN_训练.py`
- 预测脚本：`模型脚本/物理信息CNN_预测.py`
- 结果目录：`pi_cnn_nuf_results/`
- 结构证据：
  - 输入为速度、压力、温度 PNG 云图与六参数。
  - 包含 CBAM 注意力。
  - 包含 FieldHead 温度场重建分支。
  - 损失函数包含 `loss_nu + loss_f + alpha * loss_field + lam * loss_phys`。
- 用途：
  - 物理场特征学习。
  - 预测模型对比。
  - 解释注意力和物理约束的贡献。
- 限制：
  - 推理阶段需要 CFD 云图，因此不能作为差分进化优化的主适应度模型。
  - 七参数版论文的优化主线不得把该模型写成主代理模型；优化阶段采用参数向量 + 几何掩码图。

## 4. 七参数几何掩码代理优化流程

- 目录：`七参数_几何掩码代理优化/`
- LHS 采样：`七参数_几何掩码代理优化/samples/samples_7param.csv`
- 七参数标签：`七参数_几何掩码代理优化/samples/labels_7param.csv`
- 数据划分文件：`七参数_几何掩码代理优化/samples/dataset_split_7param.csv`，训练集/验证集/测试集为375/75/50；`05_训练参数_几何掩码代理模型.py`和`09_消融实验_PI_CNN_CBAM.py`默认使用该本地划分文件，不读取旧六参数结果目录中的划分文件。
- 非零 `theta` 待补跑样本：`七参数_几何掩码代理优化/samples/samples_7param_theta_supplement.csv`
- 非零 `theta` 合并标签预期输出：`七参数_几何掩码代理优化/samples/labels_7param_with_theta_supplement.csv`
- 几何掩码：`七参数_几何掩码代理优化/masks/case_x_mask.png`
- 基础参数-掩码模型早期验证：`七参数_几何掩码代理优化/models/best_model.pth`、`七参数_几何掩码代理优化/models/metrics.json`。该结果不作为论文正式消融结果。
- 基础参数-掩码模型单点预测：`七参数_几何掩码代理优化/06_预测单组七参数性能.py`。该脚本默认从 `七参数_几何掩码代理优化/samples/labels_7param.csv` 中寻找与预设基准构型最接近的参考样本，并读取其 `Nu0/f0` 后计算 `eta_pred`，避免使用 `Nu0=1,f0=1` 造成综合评价因子不可比；如需复现实验中指定基准，可显式传入 `--Nu0` 和 `--f0`。
- 基础参数-掩码模型差分进化结果：`七参数_几何掩码代理优化/optimization_results/best_params.json`
- 基础参数-掩码模型COMSOL复核：`七参数_几何掩码代理优化/validation_results/final_validation_report.json`
- 复核云图：
  - `七参数_几何掩码代理优化/validation_results/comsol/case_1/cfd_solution/velocity_magnitude.png`
  - `七参数_几何掩码代理优化/validation_results/comsol/case_1/cfd_solution/temperature.png`
  - `七参数_几何掩码代理优化/validation_results/comsol/case_1/cfd_solution/pressure.png`
- 当前事实：
  - 七参数 COMSOL 复核已跑通。
  - `ablation_results/` 已包含 30 epoch 第一版消融实验结果。
  - `optimization_results_pi_cbam/` 和 `validation_results_pi_cbam/` 已包含 PI-CNN-CBAM 几何掩码模型优化与 COMSOL 复核结果。
  - 当前训练标签中 `theta` 仅为 0，因此 PI-CNN-CBAM 优化阶段已自动固定 `theta=0`，不能写成真实七参数全空间优化。
  - 已生成60组非零 `theta` 补充样本输入，`case_id=10001` 至 `10060`，`|theta|>=2 deg`。该文件是后续COMSOL补跑入口，不能单独写成已完成非零倾斜角结果。
  - 已用 `02_七参数COMSOL批量求解.py --dry-run` 生成非零 `theta` 补跑调度计划：`七参数_几何掩码代理优化/comsol_results_theta_supplement/dry_run_plan.csv`。
  - 已新增一套独立的正式500组七参数含倾斜角采样入口：`七参数_几何掩码代理优化/samples/samples_7param_theta500_v2.csv`，`case_id=20001` 至 `20500`，`theta=-9.9675 deg` 至 `9.9808 deg`。该文件不覆盖旧500组标签，也不覆盖旧六参数数据。
  - 已用 `02_七参数COMSOL批量求解.py --dry-run` 为 `theta500_v2` 生成调度计划：`七参数_几何掩码代理优化/comsol_results_theta500_v2/dry_run_plan.csv`，计划样本数500组。该计划尚未调用COMSOL，不能写成已完成CFD数据集。
  - 已对 `theta500_v2` 前3组进行真实COMSOL求解冒烟测试：`case_20001` 至 `case_20003`，结果汇总为 `七参数_几何掩码代理优化/comsol_results_theta500_v2/summary.csv`。3组均成功，三张云图文件均通过大于20KB的有效性检查；该结果只能证明新500组链路可运行，不能替代完整500组正式训练标签。
  - `02_七参数COMSOL批量求解.py` 已增强 `image_complete` 判定：现在要求速度、压力、温度三张图均存在且文件大小不小于20000 bytes，避免空白图被误判为完整云图。
  - 已完成35组非零 `theta` 小批量COMSOL smoke 求解：`case_10001` 至 `case_10035`，结果文件位于 `七参数_几何掩码代理优化/comsol_results_theta_supplement/case_*/result.json`。
  - 累计补充结果表：`七参数_几何掩码代理优化/comsol_results_theta_supplement/summary_all.csv`。
  - 非零 `theta` 统计文件：`七参数_几何掩码代理优化/comsol_results_theta_supplement/theta_supplement_stats.json` 和 `theta_supplement_stats.md`。
  - 当前35组结果范围：`theta=-9.9317 deg` 至 `9.4690 deg`，`Nu=23.2111` 至 `45.7966`，`f=0.053424` 至 `0.151761`。逐项结果见 `summary_all.csv`。
  - 已新增安全合并脚本：`七参数_几何掩码代理优化/15_合并theta补充标签.py`。当前已生成 `七参数_几何掩码代理优化/samples/labels_7param_with_theta_supplement.csv`，其中基础样本500组，非零 `theta` 成功补充样本35组，合并样本535组。
  - 已基于合并标签重新生成几何掩码图，新增样本对应掩码为 `七参数_几何掩码代理优化/masks/case_10001_mask.png` 至 `case_10035_mask.png`。
  - 由于当前非零 `theta` 真实样本只有35组，该结果可证明补跑链路稳定并作为后续轻量重训输入，但仍不能单独支撑非零 `theta` 的敏感性规律或正式全七参数优化结论。
  - 论文正式模型评价应使用 `ablation_results/ablation_summary.csv`；`models/metrics.json` 只是早期基础模型验证记录，不能写入正式结论。

七参数版论文对应关系：

- 第4章图4-1使用：`大论文初稿/figures/fig4_1_7param_picnn_cbam_architecture.svg`，属于方法架构示意图，不是训练结果图；图内逻辑应与第5章差分进化优化保持一致，即优化阶段只输入七参数和几何掩码，不输入CFD云图。
- 第4章表4-2使用：`七参数_几何掩码代理优化/ablation_results/ablation_summary.csv`
- 第4章消融柱状图可用：`七参数_几何掩码代理优化/ablation_results/ablation_bar.png`
- 第4章各模型散点图：
  - `ablation_results/param_mlp/prediction_scatter.png`
  - `ablation_results/mask_cnn/prediction_scatter.png`
  - `ablation_results/fusion/prediction_scatter.png`
  - `ablation_results/pi_cnn_cbam/prediction_scatter.png`
- 第4章各模型损失曲线：
  - `ablation_results/param_mlp/loss_curve.png`
  - `ablation_results/mask_cnn/loss_curve.png`
  - `ablation_results/fusion/loss_curve.png`
  - `ablation_results/pi_cnn_cbam/loss_curve.png`
- PI-CNN-CBAM 中“PI”的当前实现依据：`七参数_几何掩码代理优化/09_消融实验_PI_CNN_CBAM.py` 中的 `physics_loss`。该损失约束预测 `Nu`、`f` 与由二者计算得到的 `eta` 保持一致，并惩罚非物理负值输出；它不是控制方程 PDE 残差，也不等同于完整 PINN 求解。
- 第5章PI-CNN-CBAM优化结果使用：`optimization_results_pi_cbam/best_params.json`
- PI-CNN-CBAM 单点预测脚本：`七参数_几何掩码代理优化/10_预测_PI_CNN_CBAM性能.py`。该脚本默认从 `七参数_几何掩码代理优化/samples/labels_7param.csv` 中寻找与预设基准构型最接近的参考样本，并读取其 `Nu0/f0` 后计算 `eta_pred`；如需复现实验中指定基准，可显式传入 `--Nu0` 和 `--f0`。
- 第5章COMSOL复核报告使用：`validation_results_pi_cbam/final_validation_report.json` 和 `validation_results_pi_cbam/final_validation_report.md`
- 第5章复核云图使用：`validation_results_pi_cbam/comsol/case_1/cfd_solution/`

七参数K折交叉验证状态：

- 七参数K折脚本：`七参数_几何掩码代理优化/12_七参数PI_CNN_CBAM_K折验证.py`
- 最小验证：已用2折、1轮、32像素输入跑通脚本链路；该 smoke 结果已删除，不能作为论文结果。
- 正式5折结果：已生成 `七参数_几何掩码代理优化/kfold_results/kfold_metrics.csv`、`kfold_summary.json`、`kfold_summary.md`。
- 正式配置：5折，500个样本；每折100个测试样本，剩余400个样本划分为340个训练样本和60个验证样本；训练30轮，batch size=64，image size=64。
- 汇总指标：`Nu_R2=0.9652±0.0100`，`Nu_MAE=0.3558±0.0624`，`f_R2=0.9450±0.0135`，`f_MAE=0.000777±0.000106`，`eta_R2=0.9663±0.0104`，`eta_MAE=0.0131±0.00235`。
- 写作限制：K折结果仍基于 `theta=0` 子空间样本，只能支持当前样本范围内的泛化稳定性，不能证明非零倾斜角构型的预测可靠性。

非零 `theta` 轻量重训评估状态：

- 评估脚本：`七参数_几何掩码代理优化/17_合并theta轻量重训评估.py`
- 输出目录：`七参数_几何掩码代理优化/theta_retrain_eval/`
- 汇总文件：`七参数_几何掩码代理优化/theta_retrain_eval/theta_retrain_eval_summary.json` 和 `theta_retrain_eval_summary.md`
- 数据划分：原始 `theta=0` 样本500组；非零 `theta` 样本35组，其中30组用于增强训练，5组作为非零 `theta` 保留测试集。
- 对比口径：
  - `base_only`：只用原始500组 `theta=0` 标签训练；
  - `augmented`：用原始训练集加30组非零 `theta` 标签训练；
  - 两者均在同一5组非零 `theta` 保留测试集上评估。
- 当前结果：
  - `Nu_MAE`：由13.5415降至2.31402；
  - `Nu_R2`：由-14.4238升至0.343801；
  - `f_MAE`：由0.0589412降至0.007871；
  - `f_R2`：由-5.11975升至0.843109。
- 可写结论：
  - 少量非零 `theta` 样本已经明显降低模型在非零倾斜角保留测试集上的误差，说明继续补充非零 `theta` 样本具有实际价值。
  - 该评估不是正式消融实验，也不是K折交叉验证；由于非零 `theta` 保留测试集只有5组，不能写成最终七参数模型精度结论。

## 5. 七参数 COMSOL 复核结果

来源：`七参数_几何掩码代理优化/validation_results/final_validation_report.json`

定位：早期链路验证结果，不作为七参数版论文正式优化结论。正式结论见下节 `5.1 PI-CNN-CBAM 几何掩码消融与复核结果`。

- 最优候选参数：
  - `Ta = 0.008532`
  - `Twa = 0.200000`
  - `Tb = 0.080000`
  - `Ts = 1.214044`
  - `Tt = 1.182383`
  - `Tad = 0.466368`
  - `theta = 9.272359 deg`
- 代理预测：
  - `Nu_pred = 18.9362`
  - `f_pred = 0.034585`
  - `eta_pred = 0.93636`
- COMSOL 复核：
  - `Nu = 27.541989`
  - `f = 0.071115`
  - `delta_p = 1.06672 Pa`
  - `delta_T = 18.17323 K`
  - `Q_total = 1301.36999`
  - `eta_cfd = 1.07100`
- 结论限制：
  - 该结果说明流程可闭环。
  - 由于模型只训练了 1 epoch 且标签主要来自 `theta=0` 扩展数据，不能作为正式七参数优化结论。

## 5.1 PI-CNN-CBAM 几何掩码消融与复核结果

消融结果来源：`七参数_几何掩码代理优化/ablation_results/ablation_summary.csv`

| 模型 | Nu_R2 | Nu_MAE | f_R2 | f_MAE | eta_R2 | eta_MAE |
|---|---:|---:|---:|---:|---:|---:|
| 参数 MLP | 0.9776 | 0.2831 | 0.9554 | 0.000723 | 0.9787 | 0.01038 |
| 几何 CNN | 0.9547 | 0.3913 | 0.8702 | 0.001211 | 0.9729 | 0.01137 |
| 普通融合模型 | 0.9757 | 0.2997 | 0.9534 | 0.000766 | 0.9770 | 0.01069 |
| PI-CNN-CBAM 融合模型 | 0.9806 | 0.2860 | 0.9445 | 0.000864 | 0.9804 | 0.00997 |

可写结论：

- PI-CNN-CBAM 在 `Nu_R2` 和 `eta_R2/eta_MAE` 上表现最好，说明 CBAM 与物理约束有助于综合性能预测。
- `f_R2` 最高的是参数 MLP，说明当前几何掩码分支对阻力细节的贡献有限，不能夸大图像分支对 f 的提升。
- 当前标签中 `theta=0`，因此该消融实验本质上验证的是六参数样本扩展后的七输入模型流程，不能证明非零 theta 的泛化能力。

PI-CNN-CBAM 优化结果来源：`七参数_几何掩码代理优化/optimization_results_pi_cbam/best_params.json`

- `Ta = 0.0500`
- `Twa = 0.4621`
- `Tb = 0.1264`
- `Ts = 1.5000`
- `Tt = 1.2000`
- `Tad = 0.3462`
- `theta = 0.0000`
- 代理预测：`Nu_pred = 26.1968`, `f_pred = 0.05378`, `eta_pred = 1.1181`
- 当前二维优化评价使用的参考基准样本来自 `七参数_几何掩码代理优化/samples/labels_7param.csv` 中最接近预设基准构型的一行：`case_id=464`，`Nu0=20.72776`，`f0=0.03723947`，`delta_p=0.558592 Pa`。该样本用于当前阶段计算 `eta`，不等同于预设基准构型的独立补跑结果。

COMSOL 复核来源：`七参数_几何掩码代理优化/validation_results_pi_cbam/final_validation_report.json`

- `Nu = 40.4881`
- `f = 0.087139`
- `delta_p = 1.30709 Pa`
- `delta_T = 19.57924 K`
- `Q_total = 2061.08586`
- `eta_cfd = 1.47131`
- 云图目录：`七参数_几何掩码代理优化/validation_results_pi_cbam/comsol/case_1/cfd_solution/`

可写结论：

- PI-CNN-CBAM 几何掩码模型给出的优化候选经 COMSOL 复核后表现出较高综合性能。
- 代理模型对 Nu 和 f 绝对值存在低估，最终性能判断应以 COMSOL 复核结果为准。
- 当前优化由于正式训练数据限制固定 `theta=0`，论文中应称为“七参数框架下、`theta=0` 子空间内的 PI-CNN-CBAM 几何掩码优化复核”。已有35组非零theta样本仅用于轻量重训评估，完整非零倾斜角优化仍需进一步扩充样本并重新训练。

## 6. 三维工程模型结果

- 当前七参数版正式采用的新三维结果表：`comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/results/seven_param_3d_summary.csv`
- 当前七参数版正式采用的新三维结果 JSON：`comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/results/seven_param_3d_summary.json`
- 当前七参数版正式采用的新三维结果 Markdown 表：`comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/results/seven_param_3d_summary.md`
- 七参数三维建模记录：`comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/pi_cbam_best/pi_cbam_best_realistic_build_notes.json`
  - 用途：追溯第6章三维几何、边界条件、网格设置、求解器设置和选择集。
  - 当前记录：入口速度 `0.5 m/s`，入口温度 `293.15 K`，出口压力 `0 Pa`，芯片热源 `1 W`，网格 `hauto=8`，模型已求解。
- 三维建模脚本：
  - baseline：`comsol_3d_airfoil_radiator/12_真实感翼型柱散热器建模.py --case baseline --mesh-hauto 8 --solve`
  - pi_cbam_best：`comsol_3d_airfoil_radiator/13_七参数真实芯片散热器建模.py --case-name pi_cbam_best --mesh-hauto 8 --solve`
  - 指标提取：`comsol_3d_airfoil_radiator/15_提取七参数三维指标.py`
- 主要模型：
  - baseline：`comsol_3d_airfoil_radiator/generated_realistic_heat_sink/baseline/baseline_realistic_airfoil_heat_sink.mph`
  - pi_cbam_best：`comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/pi_cbam_best/pi_cbam_best_realistic_airfoil_heat_sink.mph`
- 当前事实：
  - baseline：`T_chip_avg = 310.903 K`, `T_chip_max = 312.712 K`, `R_th = 17.753 K/W`, `delta_p = 0.32456 Pa`, `eta_3d = 1.000`
  - pi_cbam_best：`T_chip_avg = 309.767 K`, `T_chip_max = 311.269 K`, `R_th = 16.617 K/W`, `delta_p = 0.44411 Pa`, `eta_3d = 0.962`
- 结论限制：
  - 当前三维结果可以写“七参数候选结构降低芯片温度和等效热阻”。
  - 当前三维结果不能写成“三维综合性能最优”，因为压降增加使 `eta_3d < 1`。
  - 当前三维模型采用 `hauto=8` 粗网格验证口径，已确认几何、边界、网格和求解链路跑通；若用于正式发表或工程定量结论，仍需补三维网格无关性。
  - 三维结论应写为：候选结构具有降温潜力，但综合性能受流阻代价制约，后续需要三维独立优化。

## 7. 已有图件

- 主图目录：`大论文初稿/figures/`
- 二维小论文图：`大论文初稿/小论文图片/`
- 七参数复核云图：`七参数_几何掩码代理优化/validation_results/comsol/case_1/cfd_solution/`
- 七参数三维图件：`comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/pi_cbam_best/exports/`
- 风险：
  - 部分图件是示意图，不是 COMSOL 真实结果。
  - 部分图件可能来自旧六参数结果，与七参数流程不一致。
  - 正文插图前必须逐图核对来源。

## 8. 参考文献

- 中文候选：`大论文初稿/参考文献中文为主版_75篇候选.md`
- 整理库：`大论文初稿/参考文献整理库.md`
- 核验进度：`大论文初稿/参考文献核验进度.md`
- 必须包含：冯少聪. 航空翼型下的工程车辆冷却性能强化研究[D]. 华北理工大学, 2018.

## 9. 代码验证记录

最近一次本地静态验证：

- 七参数代理优化目录下全部 Python 脚本已通过 `python -m py_compile`：
  - `七参数_几何掩码代理优化/*.py`
- 三维七参数正式相关脚本已通过 `python -m py_compile`：
  - `comsol_3d_airfoil_radiator/12_真实感翼型柱散热器建模.py`
  - `comsol_3d_airfoil_radiator/13_七参数真实芯片散热器建模.py`
  - `comsol_3d_airfoil_radiator/14_导出七参数三维结果图.py`
  - `comsol_3d_airfoil_radiator/15_提取七参数三维指标.py`
- 旧六参数入口脚本已通过 `python -m py_compile`：
  - `cnnstep1.py`
  - `cnnstep2.py`
  - `cnnstep3.py`
  - `comsol单次执行脚本.py`
  - `consol500组参数.py`
- 不依赖COMSOL的几何掩码公共函数已完成烟测：
  - 参数列顺序：`Ta,Twa,Tb,Ts,Tt,Tad,theta`
  - 基准参数可通过校验；
  - `render_mask(..., image_size=64)` 可生成 `L` 模式二值掩码；
  - 基准 `eta_value(Nu0, f0, Nu0, f0)=1.0`。
- 七参数项目完整性检查脚本已通过语法检查并运行：
  - 脚本：`七参数_几何掩码代理优化/13_七参数项目完整性检查.py`
  - 报告：`七参数_几何掩码代理优化/audit_results/integrity_report.md`
  - 图表追溯清单：`七参数_几何掩码代理优化/audit_results/figure_table_trace.csv`
  - 轻量冒烟测试结果：`七参数_几何掩码代理优化/audit_results/smoke_test_results.json`
  - 当前状态：`pass`
  - 错误数：0
  - 警告数：3，分别对应 `theta=0`、`f_R2` 最高模型为参数MLP、三维 `eta_3d<1`。
  - 当前统计：标签样本500组，论文图片25张且缺失0张，论文关键数值匹配12/12，参考文献80篇，其中中文44篇、英文36篇。
- 2026-06-18 已重新验证：
  - `七参数_几何掩码代理优化/*.py` 全部通过 `python -m py_compile`。
  - `18_七参数轻量冒烟测试.py` 运行结果为 `pass`，9/9 个安全入口验证通过；该测试不训练模型、不运行COMSOL。
  - `13_七参数项目完整性检查.py` 运行结果为 `pass`，错误数0，警告数3。
  - `figure_table_trace.csv` 已生成38行记录，其中图25项、表13项；缺失图片0项、未配置证据来源0项。
  - 论文中二维COMSOL复核、三维验证和消融实验的关键数值匹配12/12。
  - 警告仍对应：`theta=0`、`f_R2` 最高模型为参数MLP、三维 `eta_3d<1`。
  - 参考文献数量仅保留统计；用户已要求文献暂缓，因此不作为当前完整性检查警告项。
- 2026-06-20 已核查三维倾斜角模型状态：
  - 二维非零 `theta` 复核参数存在：`七参数_几何掩码代理优化/validation_results/final_validation_report.json`，其中 `theta=9.27235911523071°`。
  - 三维建模入口 `comsol_3d_airfoil_radiator/13_七参数真实芯片散热器建模.py` 已支持 `--params-json` 和 `--theta`，可独立生成带倾斜角工况。
  - 当前正式三维结果 `generated_7param_chip_heat_sink/pi_cbam_best` 的 `theta=0`，不是带倾斜角三维模型。
  - 已生成带倾斜角三维模型：`comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/tilted_theta9/tilted_theta9_realistic_airfoil_heat_sink.mph`。
  - `tilted_theta9` 已完成几何、物理场、研究和网格生成；建模记录中 `mesh_generated=true`、`plot_created=true`、网格失败项为空、求解器设置失败项为空。
  - 完整性检查已自动读取该建模记录和几何预览图，并确认三维非零 `theta` 候选模型存在、建模记录存在、网格已生成、预览图存在且文件大小满足有效图件阈值。
  - 已用 `comsol_3d_airfoil_radiator/16_导出三维倾斜角几何预览.py` 生成非COMSOL求解依赖的几何检查图：
    - `comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/tilted_theta9/exports/tilted_theta9_geometry_preview.png`，104991 bytes，三维线框基座与3×8翼型柱阵列可见。
    - `comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/tilted_theta9/exports/tilted_theta9_top_view.png`，39822 bytes，俯视图中标注 `theta=9.27 deg`，可直接检查倾斜角是否进入几何。
  - `13_七参数真实芯片散热器建模.py` 与 `16_导出三维倾斜角几何预览.py` 已通过 `python -m py_compile`。
  - COMSOL内置结果图组在未求解状态下导出的 `temperature/velocity/mesh/appearance` 图片为空白图，不能作为有效图件证据；当前有效证据是 `.mph`、建模记录和上述几何预览图。
  - 完整性检查脚本已新增 `tilted_3d_export_risks` 字段，自动记录未求解导出图风险；当前检查4张、不可作为论文证据4张，文件大小均为8390 bytes：
    - `tilted_theta9_appearance.png`
    - `tilted_theta9_mesh.png`
    - `tilted_theta9_velocity.png`
    - `tilted_theta9_temperature.png`
  - 当前尚未求解，也未导出温度场、速度场和性能指标，不能写作已完成三维倾斜角性能验证。

验证边界：

- 上述验证只能说明脚本语法、公共函数和文件存在性，不等同于重新运行COMSOL求解。
- COMSOL相关结果仍以已有 `.mph`、导出图、`result.json` 和汇总CSV为准。
- 若后续修改三维几何、边界或求解器设置，应重新运行COMSOL并更新第6章结果表。
