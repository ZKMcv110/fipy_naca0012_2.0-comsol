# 七参数版当前审稿问题清单

> 检查对象：`大论文初稿/基于物理信息CNN的翼型管翅片散热器论文_七参数版.md`
> 检查时间：2026-06-18
> 原则：只记录有证据支撑的问题，不把未跑结果写成已完成。
> 当前处理状态：参考文献补充已暂停，本清单仅继续维护代码、数据、图表和论文结论一致性问题。

## 1. 必须保留的限制说明

1. 正式消融、K折和差分进化优化仍基于 `theta=0` 标签；35组非零 `theta` 样本只支持轻量重训评估，不能写成完整七参数全空间优化。
2. 差分进化优化阶段使用参数向量和几何掩码图，不使用 CFD 云图；CFD 云图只用于机理解释、旧模型对比和 COMSOL 复核展示。
3. 三维模型当前为 `hauto=8` 粗网格验证口径，能说明三维建模、边界、网格和求解链路已跑通，但不能作为最终工程定量结论。
4. 三维结果显示候选结构降低芯片温度和等效热阻，但压降增加，`eta_3d=0.962`，不能写成三维综合性能最优。

## 2. 当前已完成且有证据的内容

1. 七参数样本和标签：
   - `七参数_几何掩码代理优化/samples/labels_7param.csv`
   - 当前样本数：500
2. 七参数 PI-CNN-CBAM 消融实验：
   - `七参数_几何掩码代理优化/ablation_results/ablation_summary.csv`
   - 正文表4-2数据应来自该文件。
3. 差分进化优化：
   - `七参数_几何掩码代理优化/optimization_results_pi_cbam/best_params.json`
   - 最优参数：Ta=0.05, Twa=0.462149, Tb=0.126363, Ts=1.5, Tt=1.2, Tad=0.346194, theta=0。
4. 二维 COMSOL 复核：
   - `七参数_几何掩码代理优化/validation_results_pi_cbam/final_validation_report.json`
   - CFD复核：Nu=40.488056, f=0.087139, eta=1.471313。
5. 三维代表性模型结果：
   - `comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/results/seven_param_3d_summary.csv`
   - 基准结构：Tavg=310.903 K, Rth=17.753 K/W, Δp=0.32456 Pa。
   - 候选结构：Tavg=309.767 K, Rth=16.617 K/W, Δp=0.44411 Pa, eta_3d=0.962。
6. 敏感性分析：
   - `大论文初稿/figures/fig3_13_parameter_importance.csv`
   - 当前口径为“分箱均值趋势 + 随机森林回归特征重要性”，不是严格单因素 CFD 扫描。
7. 七参数 PI-CNN-CBAM K 折交叉验证：
   - `七参数_几何掩码代理优化/kfold_results/kfold_summary.md`
   - 5折结果：Nu R²=0.9652±0.0100，f R²=0.9450±0.0135，η R²=0.9663±0.0104。
   - 该结果已写入七参数版论文第4章表4-3。

## 3. 当前仍未完成的硬缺口

1. 中文参考文献比例不足。
   - 当前七参数版参考文献总数为80条，数量已达到70～80篇目标区间上限。
   - 含中文字符文献统计为44条，仍未达到“中文约50条”的目标。
   - 已建立候选补充清单：`大论文初稿/中文参考文献候选补充清单.md`，当前已补充16条中文候选，其中部分已核实完整题录；本轮已继续用核验来源替换弱相关英文条目。
   - 已核验进度表：`大论文初稿/参考文献核验进度.md`，新增记录了车辆散热器、翅片管换热器、异型管换热器、百叶窗翅片和平行流换热器等来源页。
   - 用户当前要求文献先不处理，因此该项保留为待办，不继续补充或替换参考文献。
   - 后续必须联网或通过可核验数据库补充，不能编造作者、题名、来源和年份。
2. 三维网格无关性未完成。
   - 当前三维结果只能写为初步工程验证。
   - 若要强化三维定量结论，需要至少补粗、中、细三组网格对 Tavg、Rth、Δp 的影响。
3. 非零倾斜角 theta 样本已有小批量补充，但仍未形成正式训练数据集。
   - 当前已完成35组非零 `theta` 的COMSOL smoke 求解：`case_10001` 至 `case_10035`。
   - 35组结果范围为：`theta=-9.9317°` 至 `9.4690°`，`Nu=23.2111` 至 `45.7966`，`f=0.053424` 至 `0.151761`。
   - 已生成合并标签 `labels_7param_with_theta_supplement.csv`，共535组，其中非零 `theta` 成功样本35组。
   - 已完成轻量重训评估：30组非零 `theta` 用于增强训练，5组非零 `theta` 作为保留测试集。加入非零 `theta` 样本后，保留测试集 `Nu_MAE` 由13.5415降至2.31402，`f_MAE` 由0.0589412降至0.007871，说明补样对倾斜角适应性有明显帮助。
   - 该结果可以证明非零倾斜角补跑链路稳定，并说明继续补样具有价值；但该评估不是正式消融实验或K折验证，仍不能单独讨论非零 theta 对尾迹、换热和阻力的真实规律。
   - 若老师要求“七参数优化”，需要继续补包含非零 theta 的 COMSOL 样本并重新训练/优化。
4. 参考文献真实性尚未逐条核验。
   - 论文已有80条参考文献，但必须逐条核对作者、题名、来源、年份。
   - 当前不能承诺所有参考文献都已通过权威来源核验。
   - 正文中新增中文条目已完成部分题录核对，但仍需通过学校数据库或期刊官网继续逐条复核。

## 4. 建议下一步

当前优先级调整为：继续扩充非零 `theta` COMSOL样本，使补充样本达到正式重训和K折验证规模；其次补三维网格无关性。参考文献补充暂停，待用户重新要求后再处理。
