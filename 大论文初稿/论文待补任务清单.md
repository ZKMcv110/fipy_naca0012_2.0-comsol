# 论文待补任务清单

## 一级优先级：必须先改，否则会被老师直接质疑

1. **修正七参数模型定位**
   - 状态：已完成第一版。
   - 已在 `七参数_几何掩码代理优化/09_消融实验_PI_CNN_CBAM.py` 中加入 CBAM 注意力和物理约束损失。
   - 已生成第一版消融指标：`七参数_几何掩码代理优化/ablation_results/ablation_summary.csv`。
   - 剩余限制：训练标签中 `theta` 只有 0，不能证明非零倾斜角影响规律。

2. **补消融实验**
   - 至少包含：
     - 参数 MLP
     - 几何 CNN
     - 普通参数-几何融合模型
     - PI-CNN-CBAM 参数-几何融合模型
   - 必须使用同一数据划分。
   - 输出表格、柱状图、散点图、损失曲线。
   - 状态：已完成 30 epoch 第一版。
   - 结果路径：`七参数_几何掩码代理优化/ablation_results/`。
   - 七参数版第4章已改为四模型消融主线：参数MLP、几何掩码CNN、参数-掩码融合模型、PI-CNN-CBAM。
   - 论文可用结论：PI-CNN-CBAM 在 Nu 和 eta 上最好，f 预测参数 MLP 更优，不能夸大为所有指标全面最优。

3. **修正差分进化描述**
   - 状态：七参数版第5章已修正。
   - 当前写法：参数生成几何掩码，代理模型预测 Nu/f，DE 计算 eta。
   - 旧 CFD 云图 PI-CNN-CBAM 只作为对比和机理解释。

4. **修正七参数优化结论**
   - 状态：七参数版第5章已修正为谨慎结论。
   - 当前 `models/metrics.json` 为早期 1 epoch 验证结果，不能作为论文正式结果。
   - 当前 COMSOL 复核与代理预测差距较大，不能写“误差小于 1%”。
   - 当前 PI-CNN-CBAM 优化与复核已完成：`optimization_results_pi_cbam/`、`validation_results_pi_cbam/`。
   - 由于训练标签中 theta 只有 0，当前优化已固定 theta=0，不能称为完整七参数全空间优化。

5. **修正三维结论**
   - 状态：七参数版第6章已按新三维结果修正，并已整理为“应用场景与散热需求—三维代表性模型—边界条件与求解设置—流热性能对比—迁移适用性分析—本章小结”结构。
   - 新三维结果来源：`comsol_3d_airfoil_radiator/generated_7param_chip_heat_sink/results/seven_param_3d_summary.csv`。
   - 当前七参数候选结构相较 baseline：芯片平均温度 310.903 K → 309.767 K，等效热阻 17.753 K/W → 16.617 K/W，压降 0.32456 Pa → 0.44411 Pa，`eta_3d=0.962`。
   - 推荐写法：三维模型显示七参数候选结构具有降温潜力，但压降代价更高，不能写成三维综合最优；三维全参数优化作为后续工作。
   - 剩余：当前三维模型为 `hauto=8` 粗网格验证口径，正式送审前若要强化三维定量结论，应补三维网格无关性或明确写成初步工程验证。

## 二级优先级：影响论文完整性

6. **补 CPU/电子芯片散热背景**
   - 第1章应从高热流密度芯片、服务器、功率器件、紧凑风冷通道切入。
   - 说明翼型扰流柱/翼型管结构的工程意义。

7. **统一参数定义**
   - 状态：七参数版已完成第二轮统一。
   - 原 md 不再继续改动，保留既有六参数版本。
   - 七参数答辩稿：`大论文初稿/基于物理信息CNN的翼型管翅片散热器论文_七参数版.md`。
   - 需要区分：
     - 六参数旧流程：`Ta,Twa,Tb,Ts,Tt,Tad`
     - 七参数新流程：新增 `theta`
   - 七参数不是直接替换旧六参数结果，属于新增优化流程。
   - 当前关键限制：真实标签中 `theta=0`，七参数版应表述为七参数框架下的 `theta=0` 子空间验证。
   - 已修正摘要、第3章参数趋势单位表述、第7章结论与创新点，避免继续使用“GAP/五模型”旧主线。

8. **统一图表来源**
   - 状态：已完成第一轮正文插图和追踪清单同步。
   - 七参数版正文现有 25 个 Markdown 图片链接，均已验证文件存在。
   - `大论文初稿/figures/` 中示意图、COMSOL图、旧结果图仍需最终人工视觉核对。
   - 已建立图表清单：图号、图名、文件路径、真实/示意、是否可用于正文。
   - 已补充数据证据链：`大论文初稿/论文数据证据链.md`。
   - 剩余：逐图人工检查图内文字、颜色条、图题是否完全匹配七参数版正文；尤其是技术路线图、参数定义图和三维图是否需要重新绘制。

8.1 **修正参数敏感性分析口径**
   - 状态：已完成。
   - 已将第3章敏感性分析改为“样本分箱均值趋势 + 随机森林特征重要性”，不再写成严格单因素CFD响应。
   - 已按 `fig3_13_parameter_importance.csv` 修正表3-2真实排序：Nu主导参数为Tt和Ts，f主导参数为Tb，η主导参数为Tt和Ts。
   - 限制：当前 `theta=0`，无法评价非零倾斜角敏感性；若老师要求严格单因素结论，应后续补代理模型受控扫描或COMSOL单因素扫描。

8.2 **补七参数K折交叉验证**
   - 状态：已完成正式5折结果。
   - 当前已有 `run_feature_fusion_kfold.py` 和 `ai_cnn_model_results/feature_fusion_kfold_5/`，但这是旧特征融合流程的5折验证结果，不能直接作为七参数几何掩码PI-CNN-CBAM模型的K折结果。
   - 已新增脚本：`七参数_几何掩码代理优化/12_七参数PI_CNN_CBAM_K折验证.py`。
   - 已完成最小链路验证：2折、1轮、32像素输入可运行；验证输出已删除，不能作为论文正式结果。
   - 正式运行命令：`python "七参数_几何掩码代理优化\12_七参数PI_CNN_CBAM_K折验证.py" --k-folds 5 --epochs 30 --batch-size 64 --image-size 64`。
   - 正式输出目录：`七参数_几何掩码代理优化/kfold_results/`。
   - 正式结果：`Nu_R2=0.9652±0.0100`，`f_R2=0.9450±0.0135`，`eta_R2=0.9663±0.0104`。
   - 七参数版第4章已补入表4-3和K折分析。
   - 限制：K折结果仍基于 `theta=0` 子空间样本，不能证明非零倾斜角泛化能力。

9. **补三维工程模型说明**
   - 状态：已完成第一轮。
   - 已明确三维模型包含：芯片热源、基板、翼型扰流柱、流体域、入口、出口、固体-流体耦合。
   - 已检查并修正边界条件、网格和求解器设置：入口改为普通速度入口；空气域采用风道减去固体的布尔差集；三维翼型截面点数降至31以保证体网格稳定；显式建立自由四面体网格。
   - 三维只写“工程可行性验证、降温潜力和流阻代价”，不要写“三维全局最优”。

10. **整理参考文献**
    - 状态：已建立 `大论文初稿/参考文献审查清单.md`。
    - 当前七参数版参考文献共80条，其中含中文字符文献约44条，数量达到70～80条目标，但中文约50条目标尚未达到。
    - 已新增中文候选清单：`大论文初稿/中文参考文献候选补充清单.md`，当前仅记录已找到来源页的候选文献，不能直接当最终参考文献表。
    - 核验中文为主参考文献。
    - 确保冯少聪 2018 进入正文引用。
    - 当前冯少聪2018已进入正文第1章翼型管工程背景处引用。
    - 不使用无法核验的作者、题名、来源、年份。

10.1 **补非零 theta 样本**
    - 状态：已生成待补跑样本输入，尚未完成COMSOL求解。
    - 新增脚本：`七参数_几何掩码代理优化/14_生成非零theta补充样本.py`。
    - 样本文件：`七参数_几何掩码代理优化/samples/samples_7param_theta_supplement.csv`。
    - 当前样本数：60组，`case_id=10001` 至 `10060`，`|theta|>=2 deg`。
    - 该文件不能作为训练标签；必须先用COMSOL批量求解并重建新标签，再重新训练、K折、优化和复核。

## 三级优先级：答辩呈现

11. **答辩讲稿**
    - 状态：已完成七参数主线第一轮同步。
    - 重点讲清：
      - 为什么是 CPU/电子芯片散热背景
      - 为什么需要翼型结构
      - 为什么预测 Nu 和 f
      - 为什么优化阶段不用 CFD 云图
      - 旧 PI-CNN-CBAM 和新几何掩码 PI-CNN-CBAM 的分工
      - 三维验证说明什么
    - 已同步文件：
      - `大论文初稿/毕业答辩PPT制作文档_可复制到其他平台.md`
      - `大论文初稿/毕业答辩PPT_下午汇报讲稿与问答.md`

12. **老师可能提问**
    - 状态：已完成第一轮补充。
    - 差分进化是不是遗传算法
    - 物理约束体现在哪里
    - 注意力机制有什么用
    - 二维最优能不能推广到三维
    - 当前三维结果是否足以证明优化有效
    - 已补充中文参考文献比例不足、冯少聪2018引用、三维结果没有明显提升等问题。
    - 已同步文件：
      - `大论文初稿/老师可能提问与回答.md`
      - `七参数_几何掩码代理优化/DEFENSE_QA.md`

## 当前建议执行顺序

1. 人工视觉核对七参数版正文中的25张图，优先检查技术路线图、参数定义图和三维图是否需要重绘。
2. 按 `参考文献审查清单.md` 检查参考文献是否真实可核验，并补足中文工程车辆/管翅式换热器/机器学习优化文献。
3. 根据最终图表版本整理答辩PPT页图，重点解释 `theta=0` 子空间限制和三维验证边界。
4. 最后通读第1到7章，检查每章小结是否都是结果型结论。
