# 论文送审证据链检查

## 当前主稿

- 主稿文件：`大论文初稿/完整论文初稿.md`
- 当前写作口径：二维部分作为主要优化结果；三维部分作为工程代表性模型迁移验证。
- 第5章结构对比口径：基准结构 vs 当前已求解代表性优化构型。

## 数据来源核对

1. 二维CFD样本数据
   - 来源目录：`consol_cfddata/case_*_cfd_solution/result.json`
   - 标签文件：`consol_cfddata/labels.csv`
   - 标签重建脚本：`data_processing/rebuild_labels_from_results.py`

2. 二维代理模型结果
   - 多模态特征融合模型：`train_multimodal_feature_model.py`
   - 纯几何MLP对照：`train_mlp_baseline.py`
   - 五折交叉验证：`ai_cnn_model_results/feature_fusion_kfold_5/feature_fusion_kfold_summary.json`
   - 正文指标采用：Nu、f、R2、MAE、RMSE及五折统计结果。

3. 二维COMSOL标量结果图
   - 数据来源：`consol_cfddata/labels.csv`
   - 绘图脚本：`data_processing/plot_comsol_result_evidence.py`
   - 已生成图：`figures/fig3_14_comsol_scalar_evidence.svg`
   - 用途：说明COMSOL结果不仅用于云图展示，也可用于Nu、f、压降和温升等标量证据链分析。

4. 二维优化复核结果
   - 当前二维最优参数：`[0.035, 0.40, 0.12, 1.10, 0.85, 0.50]`
   - 正文评价指标：Nu、翼型管平均温升、单位深度等效热阻 `R'_th`、综合评价因子 `η`。
   - `Q'_total` 仅作为辅助能量指标，不作为主要散热增强结论。
   - 风险：表4-9当前数值可追溯到 `paper_figures_svg/new_model_summary.csv`，但该CSV由图件生成脚本中的硬编码指标生成；送审前应补充对应COMSOL原始结果文件，或用可追溯CSV/JSON重新生成表4-9。

5. 三维代表性模型结果
   - 三维模型脚本：`comsol_3d_airfoil_radiator/build_airfoil_pillar_heat_sink.py`
   - 当前第5章采用已求解三维结果作为代表性优化构型结果。
   - 后续若要证明第4章二维最优参数的三维迁移效果，必须重新计算真实二维最优参数对应的三维模型，并替换表5-5、图5-4、图5-5、图5-6、图5-7及相关结论。
   - 此前由现有 `pillar_heat_sink_summary.csv` 生成的 `figures/fig5_8_3d_numeric_evidence.svg` 与当前第5章暂用口径不一致，已删除且不再使用。

## 已完成检查

- 主稿图片链接已检查，当前Markdown引用的图片文件均存在。
- 主稿中未检出旧三维搜索编号、三维寻优结论、旧交错构型名称等不符合当前口径的表述。
- 主稿中已将 `CNN`、`CBAM` 限定为图4-1、图4-2中的结构示意，不把它们写成当前定量结果的唯一实算模型；表4-3至表4-5仍以实际训练结果为准。
- 第5章当前只写“代表性优化构型”，不写三维寻优结论，也不写成第4章二维最优参数真实三维复算结果。

## 送审前必须处理

1. 重新计算二维最优参数 `[0.035, 0.40, 0.12, 1.10, 0.85, 0.50]` 对应的三维模型结果。
2. 用真实三维结果替换表5-5、图5-4、图5-5、图5-6、图5-7及第5章结论段。
3. 检查第5章COMSOL截图是否全部来自同一三维模型、同一视角和同一后处理方式。
4. 导出Word或PDF后检查图表跨页、目录、编号、参考文献格式和符号显示。
5. 将主稿参考文献由当前35篇扩充为70～80篇前，必须逐条核验候选库题录；未核验文献不得直接写入正式参考文献表。
