# 项目总说明

本项目现在包含三条主要线索：当前 COMSOL/CNN 主流程、早期 FiPy/Gmsh 流程、大论文写作材料。为了避免脚本、结果和论文草稿混在一起，已经把部分内容整理到专门文件夹。

如果后续需要重新跑数据、改模型或生成论文图，请优先查看根目录下的 `EXPERIMENT_WORKFLOW.md`。

## 1. 当前推荐目录结构

```text
fipy_naca0012_2.0/
├── comsol论文图脚本/          # COMSOL/CNN 论文图生成脚本
├── thesis_writing/            # 大论文写作工作区
├── fipy_pipeline/             # 早期 FiPy/Gmsh 仿真与旧代理模型流程
├── consol_cfddata/            # 当前 COMSOL 批量仿真结果
├── csv_data/                  # 当前 COMSOL/CNN 使用的数据表
├── ai_cnn_model_results/      # 当前 CNN/MLP/多模态模型结果
├── paper_figures_svg/         # 论文图输出目录
├── 小论文图片初稿（带参数敏感性）/ # 旧小论文图片脚本及其输出结果
├── myenvs_fipynaca2.0/        # Python 虚拟环境
└── 根目录脚本                 # 当前 COMSOL/CNN 主流程脚本
```

## 2. 当前 COMSOL/CNN 主流程

根目录下这些脚本属于当前主流程，暂时建议保留在根目录：

```text
comsol单次执行脚本.py
consol500组参数.py
data_processing/rebuild_labels_from_results.py
data_processing/organize_dataset.py
data_processing/check_dataset_integrity.py
cnnstep1.py
cnnstep2.py
cnnstep3.py
cnn_model_definition.py
train_mlp_baseline.py
train_multimodal_feature_model.py
```

常用顺序如下：

```powershell
.\myenvs_fipynaca2.0\Scripts\python.exe consol500组参数.py
.\myenvs_fipynaca2.0\Scripts\python.exe .\data_processing\rebuild_labels_from_results.py
.\myenvs_fipynaca2.0\Scripts\python.exe cnnstep2.py
.\myenvs_fipynaca2.0\Scripts\python.exe cnnstep3.py
```

说明：

- `consol_cfddata/` 是 COMSOL case 结果目录，体积大但很重要。
- `csv_data/` 里有当前训练、统计和论文表格会用到的数据。
- `ai_cnn_model_results/` 里有模型权重、指标表、预测结果和训练曲线，详细说明见 `ai_cnn_model_results/README.md`。

## 3. 论文图生成

论文图脚本已经集中到：

```text
comsol论文图脚本/
```

推荐统一入口：

```powershell
.\myenvs_fipynaca2.0\Scripts\python.exe .\comsol论文图脚本\generate_comsol_svgs.py
```

只生成某一类图：

```powershell
.\myenvs_fipynaca2.0\Scripts\python.exe .\comsol论文图脚本\generate_comsol_svgs.py --only base
.\myenvs_fipynaca2.0\Scripts\python.exe .\comsol论文图脚本\generate_comsol_svgs.py --only cbam
.\myenvs_fipynaca2.0\Scripts\python.exe .\comsol论文图脚本\generate_comsol_svgs.py --only fig8
.\myenvs_fipynaca2.0\Scripts\python.exe .\comsol论文图脚本\generate_comsol_svgs.py --only heat
```

输出目录：

```text
paper_figures_svg/
```

旧版小论文带参数敏感性的图片脚本在：

```text
小论文图片初稿（带参数敏感性）/
```

运行这些脚本后，结果统一输出到：

```text
小论文图片初稿（带参数敏感性）/带参数敏感性1.0/
```

注意：请在项目根目录运行图脚本，不要先 `cd comsol论文图脚本`，否则脚本可能找不到 `consol_cfddata/`、`csv_data/` 和 `ai_cnn_model_results/`。

## 4. 大论文写作工作区

大论文材料已经集中到：

```text
thesis_writing/
```

推荐继续整理成：

```text
thesis_writing/
├── chapters/                  # 大论文各章正文
├── plan/                      # 大纲、计划、进度、任务包
├── refs/                      # 文献与证据图谱
├── figures/                   # 图表来源登记
├── tables/                    # 表格来源登记
├── source_drafts/             # 原始论文草稿
└── notes/                     # 图表、训练、证据链说明
```

建议移动进去保留的文件：

```text
academic_pre_review_committee_loaded.md
academic_pre_review_committee_loaded.docx
PAPER_FIGURES_AND_TABLES.md
HEAT_DISSIPATION_EVIDENCE.md
TRAINING_GUIDE.md
```

可以考虑删除的旧文件：

```text
academic_pre_review_committee_loaded (2).md
CNN训练指南.md
```

删除前最好确认没有独有内容。

## 5. 早期 FiPy/Gmsh 流程

早期 FiPy/Gmsh 流程已经集中到：

```text
fipy_pipeline/
```

里面包含：

```text
naca0012dat.py
网格生成器2_三角形.py
求解器*.py
generate_samples.py
run_all_cases_refactored.py
run_optimization_case_refactored.py
train_ai_models.py
ai_optimization.py
results/
solver_results/
ai_model_results/
```

如果要运行旧 FiPy 流程，请先进入该目录：

```powershell
cd fipy_pipeline
python run_all_cases_refactored.py
```

不要从根目录直接运行：

```powershell
python fipy_pipeline/run_all_cases_refactored.py
```

原因是旧脚本大量使用相对路径，直接从根目录运行会导致路径错位。

## 6. 可以清理或归档的东西

### 可以删除

```text
__pycache__/
hs_err_pid*.log
cnnstep1 copy.py
main.py
```

### 建议归档到 `archive/old_scripts/`

```text
cnnstep2 copy.py
cnnstep3 copy.py
comsol单次执行脚本 copy.py
consol500组参数 copy.py
test_single_case.py
remove_empty_folders.ps1
```

其中 `test_single_case.py` 已整理到 `comsol单工况测试/` 时，可作为 COMSOL 单工况调试入口保留，不必再归档到 `archive/old_scripts/`。

### 暂时不要删

```text
consol_cfddata/
csv_data/
ai_cnn_model_results/
paper_figures_svg/
thesis_writing/
fipy_pipeline/
myenvs_fipynaca2.0/
```

这些目录要么是当前训练与论文写作依据，要么重建成本较高。

## 7. Git 状态说明

整理文件夹后，Git 可能显示很多 `D`，这通常表示文件从旧位置移走了。后续如果要提交整理结果，应该使用：

```powershell
git add -A
git status
```

这样 Git 才能把“删除旧位置 + 新增新位置”识别为一次目录重组。

## 8. 推荐下一步

1. 把论文草稿和图表说明移动到 `thesis_writing/source_drafts/` 与 `thesis_writing/notes/`。
2. 把旧副本脚本移动到 `archive/old_scripts/`。
3. 删除 `__pycache__/` 和明确无用的重复文件。
4. 暂时不要移动 `consol_cfddata/` 和 `ai_cnn_model_results/`，避免当前训练和图表脚本路径失效。
