# 项目目录当前审查

审查时间：2026-07-08

本文档记录当前目录是否合理，以及后续继续论文、PPT、实验验证时需要遵守的边界。

## 结论

当前目录结构总体合理，可以继续冻结使用。主线目录、旧六参数流程、工具脚本、论文材料和归档材料已经分开，后续不建议再大规模移动目录。

## 当前合理的部分

| 目录 | 判断 | 说明 |
|---|---|---|
| `七参数_MLP_CNN流场重建/` | 合理 | 当前论文主线，包含 PVT 数据集、U-Net 主模型、K 折、消融、敏感性、DE、二维 CFD 复算、网格补算和证据链 |
| `七参数_几何掩码代理优化/` | 合理 | 七参数早期 COMSOL 与几何掩码流程，保留作历史和求解入口，不作为最终论文主线 |
| `comsol_3d_airfoil_radiator/` | 合理 | 三维芯片级散热器建模、后处理、三维迁移验证和三维网格补算入口 |
| `大论文初稿/` | 合理 | 当前论文正文、答辩 PPT、旧论文参考稿和写作材料集中位置 |
| `旧六参数流程/` | 合理 | 旧 MLP、CNN、CBAM、PI-CNN、数据处理和旧图件脚本集中归类 |
| `工具脚本/` | 合理 | 通用或历史入口脚本集中位置，包括旧 COMSOL 单工况、500 组参数入口和 GPU 检查 |
| `skills/` | 合理 | 外部学术写作和研究技能包集中管理，后续仍可使用 |
| `归档/` | 合理 | 旧论文材料、旧模型结果、历史答辩材料、旧图件和临时日志归档 |
| `docs/` | 合理 | 项目说明、运行入口、迁移清单、论文证据链和执行计划 |
| `tests/` | 合理 | 当前七参数主线契约测试和网格无关性判定测试 |

## 当前不建议移动的目录

- `七参数_MLP_CNN流场重建/`
- `七参数_几何掩码代理优化/`
- `七参数_PDE_PINN尝试/`
- `comsol_3d_airfoil_radiator/`
- `大论文初稿/`
- `consol_cfddata/`
- `csv_data/`
- `myenvs_fipynaca2.0/`

这些目录要么是当前论文证据链依赖，要么有大量脚本硬编码路径，要么重建成本高。

## 剩余小问题

1. `.tmp_pytest/pytest-of-20614/` 当前存在权限占用，说明有 pytest 临时目录未清理。它不影响论文和脚本运行，等相关进程释放后再清理。
2. `consol_cfddata/` 和 `csv_data/` 仍在根目录。它们属于旧数据，但不建议移动，因为旧脚本引用多、重建成本高。
3. `cnnstep1.py`、`cnnstep2.py`、`cnnstep3.py` 仍在根目录。它们是旧六参数模型兼容入口，当前可保留；若后续要彻底精简根目录，需要同步改 README。
4. `CLIProxyAPI/` 是独立服务目录，当前未纳入论文主线。除非确认不用，否则暂不归档。

## 目录冻结建议

后续只做三类动作：

1. 当前论文主线继续产出结果到 `七参数_MLP_CNN流场重建/`、`comsol_3d_airfoil_radiator/` 和 `大论文初稿/`。
2. 旧六参数或历史材料只放入 `旧六参数流程/` 或 `归档/`。
3. 新说明文档统一放入 `docs/`，不要再散落到根目录。

## 本次验证

- `python -m py_compile` 检查了七参数主线 9 个关键脚本，结果通过。
- `python "七参数_MLP_CNN流场重建\16_论文证据链汇总.py"` 可重新生成证据链总表。
- `pytest -q tests/test_7param_mainline_contracts.py tests/test_grid_independence_summary.py` 结果为 11 个测试通过。
- `python "七参数_MLP_CNN流场重建\18_论文完成度审计.py"` 按设计返回非零状态，因为正式网格无关性仍未通过；这不是目录问题。

